Project: CG-Dating-App
Current Phase: Phase 7 — Verification (complete for this pass — mobile OTP + photo/selfie, MOCK SMS delivery + manual photo review), transitioning into Phase 8 — Safety (Report/Block/Safety Center)
Current Task: Task #9 in the internal TaskList (= docs/ROADMAP.md's Phase 7, Verification) implemented and verified where possible in this sandbox — see "Next Exact Task" below
Completed Features: Project scaffold (Express backend, React/Vite/Tailwind frontend, Mongoose placeholder models, README, .gitignore) — commit fd72a17; Authentication (signup/login/JWT, protected /me) — commit 3f440ca; Profile system (model, CRUD API, completion score, multi-step profile builder UI) — commit 00c29f3; Discovery + Matching (Like/Match models, discovery feed with pagination + basic filters, swipe API with mutual-match detection, matches list, discovery/match UI) — commit f86267a; Chat (Message model, GET/POST/PATCH /api/matches/:matchId/messages, Socket.IO real-time layer with JWT handshake auth reusing the shared verifyToken() helper, typing indicator, read receipts, real chat UI replacing ChatComingSoon.jsx) — commit f027d73; Notifications (Notification model, GET/PATCH/PUT /api/notifications* routes, match/like/message notification creation with per-type preference gating, live notification:new Socket.IO event via a per-user room, bell/badge + dropdown notification center, minimal Settings page for preference toggles) — commit cc7fb76; Verification (mobileVerification/photoVerification sub-documents on User, POST /api/verification/mobile/request-otp + /verify-otp with hashed+expiring+rate-limited OTPs and MOCK/DEV-ONLY SMS delivery, POST /api/verification/photo/submit -> PENDING manual-review record reusing the existing mock photo-storage pattern, GET /api/verification/status, mobileVerified/photoVerified badges surfaced on public profile/discovery/match views, a Verification screen + reusable VerificationBadge component on the frontend) — see this entry's commit
Features In Progress: None currently in flight
Remaining Features: Safety (Report/Block/Safety Center), Admin panel, Subscription, AI features, Safe Date, Events, Analytics, Security hardening, Testing, Deployment. Also still deferred within already-started phases: a persisted `preferences` collection (age range/distance filters), unmatch, "who liked you" reveal screen (the notification exists but stays anonymous — see MOCK_FEATURES.md/docs/BUSINESS_PLAN.md), image/voice chat messages (V2), matches-list last-message preview/unread badge, real FCM push notification delivery, real SMS/OTP provider delivery (currently MOCK/DEV-ONLY — see MOCK_FEATURES.md), the photo-verification human-review queue itself (submissions reach PENDING but nothing yet transitions them to VERIFIED/REJECTED — that's the future Admin panel's job)
Known Bugs: None recorded yet — see BUGS.md
Known Technical Debt: MongoDB connection is still not verified against a live database in this sandbox (no mongod/Docker/network access to MongoDB binaries available here — see IMPLEMENTATION_PROGRESS.md for what was and wasn't verifiable) — this now also blocks verifying the User schema's new mobileVerification/photoVerification fields (including the `select: false` internal-field behavior, which was verified statically via `User.schema.path(...).options.select` rather than against a real query) against a real database, in addition to the previously-noted Notification/Like/Match/Message index gaps; `users.phone` is not unique-indexed even though the original DATABASE_SCHEMA.md draft called for it — signup never collects a phone, and Task #9's request-otp route has no cross-account uniqueness check in this MVP pass (see docs/DATABASE_SCHEMA.md's divergence note) — worth revisiting before phone becomes a login/2FA identifier; Cloudinary/Firebase/Razorpay/Claude API integrations not yet configured (no credentials yet) — Firebase specifically gates real push notification delivery, see MOCK_FEATURES.md; photo storage (both profile photos and now verification selfies) is a MOCK/TEMPORARY path (base64/URL stored directly on the document) until Cloudinary is wired up — see MOCK_FEATURES.md; discovery has no geo/distance filtering yet (profiles.location still unpopulated) — see MOCK_FEATURES.md; chat is text-only (no attachments) — not a mock, a documented scope reduction, see MOCK_FEATURES.md; SMS/OTP delivery is MOCK/DEV-ONLY (console log + dev-only response field, no real provider) — see MOCK_FEATURES.md; photo verification has no automated face-match and no reviewer UI yet — submissions reach PENDING and stay there until the future Admin panel exists
Last Successful Test: Backend server boots cleanly with all seven route groups (auth/profile/discovery/matches/notifications/verification/health) and Socket.IO mounted, no syntax/import errors. Curled every new /api/verification/* route (POST mobile/request-otp, POST mobile/verify-otp, POST photo/submit, GET status) with no/bad auth and confirmed 401 in all cases. Two standalone Node scripts (no live DB, same pattern as every prior session): (1) 38/38 checks on backend/constants/verificationOptions.js + backend/utils/verificationUtils.js + backend/utils/mockImageUpload.js in isolation, covering OTP generation entropy/format, phone masking, phone-format validation, the pure sliding-window rate-limit check (allows under the cap, blocks at the cap, prunes entries outside the 10-min window, boundary-exact pruning), OTP expiry math, the owner-safe/public-safe serializers never leaking otpHash/otpExpiresAt/otpRequestTimestamps/reviewNotes/reviewedBy/reviewedAt/submittedPhotoUrl-to-strangers, the mock image-upload validator, and Mongoose schema-level checks (enum acceptance/rejection for all 5 statuses on both sub-documents, and confirming via `schema.path(...).options.select` that the six internal fields are genuinely `select: false` while the client-safe fields are not); (2) a 33/33-check real-HTTP integration test that loads the ACTUAL backend/routes/verification.js file into a real Express app (with only backend/models/User.js swapped for an in-memory fake via require.cache injection, since MongoDB is unreachable here) and drives it over real HTTP with a real JWT — covering request-otp happy path + phone validation + rate-limit-blocks-the-4th-request-in-window + already-verified short-circuit + re-verifying-a-different-number-is-allowed, verify-otp happy path + wrong-OTP + expired-OTP-flips-to-EXPIRED + replay-of-a-used-OTP-is-rejected + missing-otp-400, photo/submit happy path + 409-while-already-PENDING + base64 storage path, and GET /status shape/leak checks. Additionally ran that same request-otp flow with NODE_ENV=production in a separate process and confirmed the `devOtp` response field is completely absent (only `message`/`phone`/`expiresInSeconds` keys present) while still being logged server-side as the MOCK SMS stand-in — directly verifies the "never in production" requirement. Frontend `npm run build` and `npm run lint` both pass with the new Verification.jsx/VerificationBadge.jsx and the badge additions to Dashboard/Discovery/Matches/Settings (same two pre-existing oxlint only-export-components warnings carried forward, both already-accepted patterns from prior sessions, no new warnings). DB-touching behavior (actual persistence of mobileVerification/photoVerification across real writes, the phone-uniqueness gap noted above, real concurrent-OTP-request races) could not be exercised end-to-end — see Known Technical Debt.
Last Modified Files: backend/constants/verificationOptions.js (new — VERIFICATION_STATUSES, OTP_LENGTH/EXPIRY/MAX_REQUESTS/RATE_LIMIT_WINDOW, PHONE_RE), backend/utils/verificationUtils.js (new — generateOtp()/maskPhone()/pruneOtpTimestamps()/isOtpRateLimited()/otpExpiryDate()/isOtpExpired()/toOwnVerificationStatusJSON()/toPublicVerificationBadges()), backend/utils/mockImageUpload.js (new — resolveMockImageUrl(), shared mock photo-storage validator factored out for reuse by both profile photos and verification selfies), backend/routes/verification.js (new — POST mobile/request-otp, POST mobile/verify-otp, POST photo/submit, GET status), backend/models/User.js (added mobileVerification/photoVerification sub-documents, several fields `select: false`), backend/routes/profile.js (GET /:userId now also fetches the owner's User doc and includes mobileVerified/photoVerified badges), backend/routes/discovery.js (feed now bulk-fetches verification badges for the page's candidates), backend/routes/matches.js (match list's otherUser now includes verification badges, bulk-fetched alongside profiles), backend/utils/profileSerializers.js (toPublicProfileJSON() now takes an optional verificationUser param and spreads in the two badge booleans), backend/server.js (mounted /api/verification), frontend/src/api.js (requestMobileOtp/verifyMobileOtp/submitPhotoVerification/getVerificationStatus), frontend/src/components/VerificationBadge.jsx (new), frontend/src/pages/Verification.jsx (new — mobile OTP flow + photo/selfie flow, both driven off server status rather than independent local step state), frontend/src/App.jsx (added /verification route), frontend/src/pages/Dashboard.jsx (own-account badges + "Get Verified"/"Verification" link), frontend/src/pages/Settings.jsx (Verification link), frontend/src/pages/Discovery.jsx (badges on discovery cards), frontend/src/pages/Matches.jsx (badges on match list cards, verified ring on Avatar), docs/DATABASE_SCHEMA.md, docs/API_DOCUMENTATION.md, docs/ROADMAP.md, MOCK_FEATURES.md, TODO.md (see git log for the latest)
Database Status: MongoDB/Mongoose schemas implemented for User (now with mobileVerification/photoVerification), Profile, Like, Match, Message, Notification; no live DB connection has ever been verified in any sandbox session so far — this remains the top technical-debt item blocking real end-to-end testing
Backend Status: Express + Socket.IO server (same HTTP server) running with auth + profile + discovery + matches + notifications + verification routes; GET/PUT /api/profile/me, GET /api/profile/:userId, POST /api/profile/me/photos, GET /api/discovery/feed, POST /api/discovery/swipe, GET /api/matches, GET/POST /api/matches/:matchId/messages, PATCH /api/matches/:matchId/messages/read, GET/PATCH/PUT /api/notifications*, POST /api/verification/mobile/request-otp, POST /api/verification/mobile/verify-otp, POST /api/verification/photo/submit, GET /api/verification/status all implemented and auth-protected; Socket.IO events unchanged from the Notifications pass
Frontend Status: Landing, auth pages, dashboard (profile completion + CTA + Discover/Matches nav + notification bell + own verification badges + Get Verified/Settings links), multi-step Profile Builder, Discovery (card + verification badges + Like/Pass, loads more as queue runs low, "It's a Match!" modal, notification bell), Matches (list + verification badges, links to the real chat screen, notification bell), Chat (message history + live delivery + typing + read receipts), Settings (notification preference toggles + Verification link), Verification (new — mobile OTP request/verify steps and photo/selfie upload-or-URL submit, both status-driven) are all live; design-system color tokens wired into Tailwind v4 via frontend/src/index.css; Button/TextField/Avatar/MatchModal/ChatBubble/NotificationBell/VerificationBadge reusable components under frontend/src/components/
Authentication Status: Implemented (signup/login/JWT/me endpoint) — unchanged in this session; still email/password only, no phone captured at signup (Verification is a separate post-signup flow, see docs/DATABASE_SCHEMA.md's `users.phone` divergence note)
AI Status: Not started (planned: Claude API, backend-only)
Payment Status: Not started (planned: Razorpay, configurable plans)
Admin Status: Not started — note for whoever builds it: the photo-verification review queue (approve/reject `photoVerification.status`, write `reviewNotes`/`reviewedBy`/`reviewedAt`) is a direct dependency Task #9 deliberately left for this phase, see docs/DATABASE_SCHEMA.md's `verifications` section
Deployment Status: Not started (planned: Render/Railway + MongoDB Atlas + Vercel/Netlify + Cloudinary)
Next Exact Task: Task #10 — Report/Block + Safety Center (per the task-launch instructions' own default; this sandbox session does not have a TaskList tool available to cross-check the internal task graph directly — ToolSearch for "TaskList" returned no such tool — so this is written per the launch instructions' explicit fallback rather than a live TaskList lookup. Whoever picks this up next should still confirm against TaskList if it's available to them, in case the graph changed in the meantime). Scope per docs/API_DOCUMENTATION.md's `[PLANNED]` Safety section and docs/DATABASE_SCHEMA.md's `reports`/`blocks` collections (both currently stub-only descriptions, not yet reviewed against real code): `POST /api/reports` (reporterId/reportedUserId/reason/details -> OPEN), `POST /api/blocks` + `DELETE /api/blocks/:blockedUserId` + `GET /api/blocks`, and — per the existing `TODO(Report/Block...)` comment already sitting in backend/routes/discovery.js's feed query — wiring blocked-user exclusion into the discovery feed once the Block model exists (both directions: don't show someone who blocked me, don't show someone I blocked). A Safety Center screen on the frontend, plus Report/Block entry points from profile views and chat, per docs/DESIGN_SYSTEM.md's Dialog component ("Unmatch this person?" pattern reference) and TODO.md's Safety section.
Next Recommended Action: Start Task #10 (Report/Block + Safety Center). Check the exact `TODO(Report/Block...)` comment in backend/routes/discovery.js's `GET /feed` handler first — it marks precisely where blocked-user exclusion needs to be added once the Block model lands. Consider also whether blocking should immediately end an active match/conversation (not currently specified anywhere in the docs) — worth a design decision up front rather than discovering it mid-implementation. Before or alongside it, get a real MongoDB connection verified in whatever environment will run this next (still blocked in every sandbox session so far — no local mongod, no Docker daemon, and mongodb-memory-server's binary download was blocked by the outbound proxy) so the growing pile of DB-dependent behavior (Notifications' real persistence, Verification's mobileVerification/photoVerification field behavior against a real query including the `select: false` fields, and soon Report/Block) can finally be exercised end-to-end instead of only via server-boot + pure-logic + fake-model-over-real-HTTP checks.
