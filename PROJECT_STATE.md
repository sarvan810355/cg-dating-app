Project: CG-Dating-App
Current Phase: Phase 8 (Safety) and Phase 9 (Admin Panel) complete/nearly-complete; this pass adds Subscription scaffolding (roughly Phase 10-equivalent in docs/ROADMAP.md's numbering — see TaskList's Task #12, a separate numbering from the Phase scheme, per this project's established note that the two don't map 1:1)
Current Task: Task #12 in the internal TaskList — Subscription scaffolding (basic): configurable Plan/Subscription models, GET /api/plans + GET/POST /api/subscription/* routes, a server-side hasFeature() entitlement helper applied to a real free-tier daily-like-limit gate on POST /api/discovery/swipe, and matching frontend (Subscription/Upgrade page, plan status + Cancel on Settings, an upgrade prompt on Discovery's 429). Built CONCURRENTLY with another background agent's Task #11 (Admin panel) working on the same branch/working tree at the same time — see "Last Modified Files" below for how the two passes' overlapping files (User.js, discovery.js, server.js, App.jsx, Settings.jsx, api.js) were kept separable in git history despite sharing a live working tree.
Completed Features: Project scaffold (Express backend, React/Vite/Tailwind frontend, Mongoose placeholder models, README, .gitignore) — commit fd72a17; Authentication (signup/login/JWT, protected /me) — commit 3f440ca; Profile system (model, CRUD API, completion score, multi-step profile builder UI) — commit 00c29f3; Discovery + Matching (Like/Match models, discovery feed with pagination + basic filters, swipe API with mutual-match detection, matches list, discovery/match UI) — commit f86267a; Chat (Message model, GET/POST/PATCH /api/matches/:matchId/messages, Socket.IO real-time layer with JWT handshake auth reusing the shared verifyToken() helper, typing indicator, read receipts, real chat UI replacing ChatComingSoon.jsx) — commit f027d73; Notifications (Notification model, GET/PATCH/PUT /api/notifications* routes, match/like/message notification creation with per-type preference gating, live notification:new Socket.IO event via a per-user room, bell/badge + dropdown notification center, minimal Settings page for preference toggles) — commit cc7fb76; Verification (mobileVerification/photoVerification sub-documents on User, POST /api/verification/mobile/request-otp + /verify-otp with hashed+expiring+rate-limited OTPs and MOCK/DEV-ONLY SMS delivery, POST /api/verification/photo/submit -> PENDING manual-review record reusing the existing mock photo-storage pattern, GET /api/verification/status, mobileVerified/photoVerified badges surfaced on public profile/discovery/match views, a Verification screen + reusable VerificationBadge component on the frontend) — commit 0d724fe; Safety (Report model + POST /api/reports with a 9-value reason enum + optional details/evidence, Block model + POST/DELETE/GET /api/blocks with idempotent create + unique-index dedup, bidirectional blocking exclusion enforced identically across the discovery feed, matches list, message REST routes, and Socket.IO match:join via a shared backend/utils/blockUtils.js, a Report/Block entry-point menu reachable from Discovery cards and Chat, a Blocked Users management screen, and a static Safety Center screen) — commit 2ab2aca; Subscription scaffolding (Task #12 — Plan model, DB-backed and idempotently-seeded at startup with the three default plans per docs/BUSINESS_PLAN.md rather than hardcoded, per the spec's admin-editable-pricing requirement; Subscription model; GET /api/plans public listing; GET /api/subscription/me, POST /api/subscription/subscribe [explicit MOCK checkout — no real Razorpay, immediate activation, clearly flagged in code comments/MOCK_FEATURES.md/docs/API_DOCUMENTATION.md], POST /api/subscription/cancel [standard SaaS: stays valid until expiresAt]; backend/utils/entitlementUtils.js#hasFeature() as the sole DB-backed entitlement check, never trusting a client claim, demonstrated by gating POST /api/discovery/swipe's free-tier daily like limit [20/day, see docs/BUSINESS_PLAN.md] behind the unlimited_likes feature via a new dailyLikeCount/lastLikeCountReset pair on User; frontend Subscription/Upgrade page, Settings Membership status+Cancel section, and a Discovery.jsx upgrade prompt on the swipe endpoint's 429 upgradeRequired response) — see this entry's commit
Features In Progress: None currently in flight
Remaining Features: AI features, Safe Date, Events, Analytics, Security hardening, Testing, Deployment (Admin panel and Subscription basic scaffolding are now both implemented per this and the concurrent pass — see Completed Features). Also still deferred within already-started phases: a persisted `preferences` collection (age range/distance filters), unmatch, "who liked you" reveal screen (the notification exists but stays anonymous — see MOCK_FEATURES.md/docs/BUSINESS_PLAN.md; `see_who_liked_you` is a stored Plan feature flag but has no gated code path yet, see Subscription's Known Technical Debt entry below), image/voice chat messages (V2), matches-list last-message preview/unread badge, real FCM push notification delivery, real SMS/OTP provider delivery (currently MOCK/DEV-ONLY — see MOCK_FEATURES.md), and — the biggest remaining gap in this pass — a **real Razorpay integration**: POST /api/subscription/subscribe is a MOCK checkout (immediate activation, no payment collected, no webhook) that must not ship to production as-is; advanced_filters/boost/incognito Plan features are likewise stored/served but not yet enforced anywhere (same "documented scope boundary" as the rest of this list)
Known Bugs: None recorded yet — see BUGS.md
Known Technical Debt: MongoDB connection is still not verified against a live database in this sandbox (no mongod/Docker/network access to MongoDB binaries available here — see IMPLEMENTATION_PROGRESS.md for what was and wasn't verifiable) — this now also blocks verifying the Block model's unique compound index and the Report model's two indexes against a real database (verified instead via a fake-model-over-real-HTTP integration script, see Last Successful Test), in addition to the previously-noted Notification/Like/Match/Message/verification index gaps; `users.phone` is not unique-indexed (unchanged from Task #9); Cloudinary/Firebase/Razorpay/Claude API integrations not yet configured (no credentials yet) — see MOCK_FEATURES.md; photo storage is a MOCK/TEMPORARY path until Cloudinary is wired up — see MOCK_FEATURES.md; discovery has no geo/distance filtering yet — see MOCK_FEATURES.md; chat is text-only — documented scope reduction, see MOCK_FEATURES.md; SMS/OTP delivery is MOCK/DEV-ONLY — see MOCK_FEATURES.md; photo verification has no automated face-match and no reviewer UI yet; report evidence is plain strings only, no file upload, and there is no automated spam/scam/abuse detection anywhere in Report/Block — both are documented as intentionally out of scope for this task (manual reporting only; automated detection is the future "Trust Engine", V3); reports and photo-verification submissions previously sat unreviewed until the Admin panel existed — now addressed by the concurrent Task #11 pass; **Subscription (Task #12, this pass): POST /api/subscription/subscribe is an explicit MOCK checkout — no real Razorpay integration, no order creation, no payment collected, no webhook verification, immediate activation on nothing more than an authenticated request — see MOCK_FEATURES.md's Razorpay entry and docs/API_DOCUMENTATION.md's §9 before this ships to real users; only the `unlimited_likes` Plan feature is actually enforced anywhere (the discovery daily-like-limit) — `advanced_filters`/`see_who_liked_you`/`boost`/`incognito` are stored/served by GET /api/plans but have no gated code path yet; the daily-like-limit's reset is UTC-calendar-day-based (not the user's local timezone, which the backend doesn't know) — see backend/utils/entitlementUtils.js; Subscription/Plan indexes (unique on Plan.code, compound on Subscription's (user, status, expiresAt)) are unverified against a real database for the same MongoDB-unreachable-in-sandbox reason as every other index in this project so far**
Last Successful Test (Task #12, this pass — earlier passes' test summaries are preserved in git history/IMPLEMENTATION_PROGRESS.md rather than accumulated here): Backend server boots cleanly (all route groups including the concurrent Task #11's admin routes, no syntax/import errors) and correctly returns 401 for GET /api/subscription/me, POST /api/subscription/subscribe, and POST /api/subscription/cancel with no/bad auth, while GET /api/plans requires no auth at all (curled directly; it 500s in this sandbox only because MongoDB is unreachable — confirmed via server logs to be a genuine 10s Mongoose buffering timeout, not a route/wiring bug). A standalone Node script (no live DB, same fake-model-over-real-HTTP-adjacent pattern as prior passes, but here faking Plan/Subscription/User's Mongoose query surface directly via require.cache injection since entitlementUtils.js is a pure logic layer, not itself an HTTP route) loaded the ACTUAL backend/utils/entitlementUtils.js and ran 15/15 checks, covering: (1) seedDefaultPlans() idempotency — first run creates all 3 plans, a second run creates none AND does not clobber a simulated admin price edit made in between; (2) hasFeature() — false for a free-tier user, true for an ACTIVE CG_PLUS subscriber (and correctly false for a CG_PRO-only feature on that same user), true for a CANCELLED-but-not-yet-expired subscriber (proving cancel doesn't immediately revoke access), false for an EXPIRED-status subscriber, and false once a still-CANCELLED-status subscriber's `expiresAt` actually passes (time-based, not just status-based); (3) tryConsumeDailyLike() — allows exactly the first 20 likes/day and blocks the 21st without incrementing further, resets on a new UTC calendar day, does NOT reset within the same UTC day, and an unlimited_likes subscriber bypasses the counter entirely (confirmed their dailyLikeCount stays untouched). Frontend `npm run build` and `npm run lint` both pass with the new Subscription.jsx page and the Settings.jsx/Discovery.jsx/api.js wiring (same two pre-existing oxlint only-export-components warnings carried forward, no new warnings). DB-touching behavior (actual Plan/Subscription persistence, the unique-index behavior under real concurrent inserts) could not be exercised end-to-end — see Known Technical Debt, unchanged root cause as every prior pass.
Last Modified Files (Task #12, this pass — see git log for the exact commit; earlier passes' file lists are preserved in git history/IMPLEMENTATION_PROGRESS.md rather than accumulated here): backend/constants/subscriptionOptions.js (new — PLAN_CODES/BILLING_PERIODS/SUBSCRIPTION_STATUSES/PLAN_FEATURES/UNLIMITED_LIKES_FEATURE/FREE_TIER_DAILY_LIKE_LIMIT/DEFAULT_PLANS seed data), backend/models/Plan.js (new), backend/models/Subscription.js (new), backend/utils/entitlementUtils.js (new — seedDefaultPlans()/getEffectiveSubscription()/hasFeature()/tryConsumeDailyLike()), backend/utils/subscriptionSerializers.js (new), backend/routes/subscription.js (new — GET /api/plans, GET/POST /api/subscription/*), backend/models/User.js (additive: dailyLikeCount/lastLikeCountReset fields), backend/routes/discovery.js (additive: POST /swipe now enforces the free-tier daily like limit via tryConsumeDailyLike() before Like.create()), backend/server.js (additive: mounts the subscription router at /api, calls seedDefaultPlans() after mongoose.connect()), frontend/src/pages/Subscription.jsx (new — plan cards + mock-checkout Subscribe button + current-plan/cancel), frontend/src/pages/Settings.jsx (additive: new MembershipSection — Free/plan-name+expiry status + Manage/Cancel), frontend/src/pages/Discovery.jsx (additive: 429 upgradeRequired handling -> upgrade prompt instead of a raw error), frontend/src/api.js (additive: getPlans/getMySubscription/subscribeToPlan/cancelSubscription, plus err.status/err.data attached to thrown request() errors so callers can branch on more than the message string), frontend/src/App.jsx (additive: /subscription route), docs/DATABASE_SCHEMA.md, docs/API_DOCUMENTATION.md, docs/BUSINESS_PLAN.md, MOCK_FEATURES.md, TODO.md (see git log for the latest). **Concurrency note:** this pass ran in the same live working tree as another background agent's Task #11 (Admin panel) at the same time (not separate clones needing a later `git pull --rebase` as originally anticipated by the task's own launch instructions — the sandbox actually shared one filesystem). For the six files both passes touched (User.js, discovery.js, server.js, App.jsx, Settings.jsx, api.js), this pass's commit stages a reconstructed "this-task's-hunks-only" blob per file (base HEAD + only this pass's own edits, verified by diffing against the live working-tree file to confirm the only remaining difference is the other agent's in-flight work) via `git hash-object`/`git update-index --cacheinfo`, rather than `git add`ing the live working-tree file directly — this keeps the two passes' authorship cleanly separable in git history despite sharing a working tree, and leaves the other agent's own uncommitted edits on disk, untouched, for them to commit themselves.
Database Status: MongoDB/Mongoose schemas implemented for User, Profile, Like, Match, Message, Notification, Block/Report, and now Plan/Subscription; no live DB connection has ever been verified in any sandbox session so far — this remains the top technical-debt item blocking real end-to-end testing
Backend Status: Express + Socket.IO server (same HTTP server) running with auth + profile + discovery + matches + notifications + verification + reports + blocks + subscription routes (plus the concurrent Task #11 pass's admin routes); GET/PUT /api/profile/me, GET /api/profile/:userId, POST /api/profile/me/photos, GET /api/discovery/feed, POST /api/discovery/swipe (now with a 429 free-tier daily-like-limit response), GET /api/matches, GET/POST /api/matches/:matchId/messages, PATCH /api/matches/:matchId/messages/read, GET/PATCH/PUT /api/notifications*, POST /api/verification/mobile/request-otp, POST /api/verification/mobile/verify-otp, POST /api/verification/photo/submit, GET /api/verification/status, POST /api/reports, POST/DELETE/GET /api/blocks, GET /api/plans, GET /api/subscription/me, POST /api/subscription/subscribe, POST /api/subscription/cancel all implemented and (except GET /api/plans, deliberately public) auth-protected; plans are idempotently seeded at startup after mongoose.connect() succeeds
Frontend Status: Landing, auth pages, dashboard, multi-step Profile Builder, Discovery (card + verification badges + Like/Pass + Report/Block menu + a daily-like-limit upgrade prompt, loads more as queue runs low, "It's a Match!" modal, notification bell), Matches (list + verification badges, links to the real chat screen, notification bell), Chat (message history + live delivery + typing + read receipts + Report/Block menu in the header), Settings (notification preference toggles + a Membership section [plan status + Cancel] + Verification/Safety Center/Blocked Users links), Verification (mobile OTP + photo/selfie flows), Blocked Users (list + unblock), Safety Center (static safety/scam-awareness content), Subscription (new — three plan cards with mock-checkout Subscribe buttons, current-plan status + Cancel) are all live; design-system color tokens wired into Tailwind v4 via frontend/src/index.css; Button/TextField/Avatar/MatchModal/ChatBubble/NotificationBell/VerificationBadge/ReportModal/SafetyMenu reusable components under frontend/src/components/
Authentication Status: Implemented (signup/login/JWT/me endpoint) — unchanged in this session
AI Status: Not started (planned: Claude API, backend-only)
Payment Status: Subscription scaffolding implemented (Task #12, this pass) — configurable admin-editable Plan model (seeded, not hardcoded), Subscription model, GET /api/plans + GET/POST /api/subscription/* routes, and a real server-side hasFeature() entitlement helper demonstrated on the discovery daily-like-limit — but the actual **payment collection is a MOCK checkout, not real Razorpay**: no credentials configured, no order creation, no webhook verification, immediate activation on an authenticated request alone. See MOCK_FEATURES.md and docs/API_DOCUMENTATION.md's §9 — this is the single biggest gap before this ships to real users.
Admin Status: Implemented concurrently by another background agent in this same pass (Task #11) — see that work's own commit/PROJECT_STATE.md entry for the authoritative writeup; from what was directly observable while this Task #12 pass ran alongside it: role/accountStatus fields on User, role-gated /api/admin/* routes + /admin frontend pages, and a login/discovery suspend-check.
Deployment Status: Not started (planned: Render/Railway + MongoDB Atlas + Vercel/Netlify + Cloudinary)
Next Exact Task: Task #6 — Final polish (per the task-launch instructions' own guidance: Task #12, the last of #9/#11/#12 blocking #6, is now complete in this pass, and #9 was already done; #11 appears complete or very nearly so as of this pass — see the "Admin Status" line above, observed live in the shared working tree — but whoever picks this up next should verify #11 actually landed a finished commit before assuming #6 is unblocked, since this pass couldn't directly confirm the other agent's completion state. This sandbox session does not have a TaskList tool available to cross-check the internal task graph directly — ToolSearch found none here either, same as every prior session's note — so this is written per the launch instructions' explicit fallback rather than a live TaskList lookup.). Scope per TODO.md's "Cross-cutting MVP work" section: security hardening pass, input validation audit across all endpoints, consistent error-response format, a basic automated test suite, and general polish — see docs/ARCHITECTURE.md and docs/TESTING_STRATEGY.md.
Next Recommended Action: Confirm Task #11's commit actually landed (check git log / PROJECT_STATE.md for an Admin-panel commit newer than this pass's), then start Task #6 (Final polish) per the scope above. Also worth folding into that pass: the growing list of Plan/Subscription-adjacent gaps noted in "Remaining Features" above (real Razorpay integration being the big one), and — unchanged from every prior pass — get a real MongoDB connection verified in whatever environment will run this next (still blocked in every sandbox session so far — no local mongod, no Docker daemon, and mongodb-memory-server's binary download was blocked by the outbound proxy) so the growing pile of DB-dependent behavior (Notifications' real persistence, Verification's select:false fields, Block's unique index, Admin's role-gating, and now Plan/Subscription's indexes and seeding) can finally be exercised end-to-end instead of only via server-boot + pure-logic + fake-model-over-real-HTTP checks.
