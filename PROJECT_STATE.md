Project: CG-Dating-App
Current Phase: Phase 6 — Notifications (complete for this pass — in-app; FCM push MOCK/TEMPORARY-deferred), transitioning into Phase 7 — Verification
Current Task: Task #8 in the internal TaskList (= docs/ROADMAP.md's Phase 6, Notifications) implemented and verified where possible in this sandbox — see the numbering note under "Next Exact Task" below
Completed Features: Project scaffold (Express backend, React/Vite/Tailwind frontend, Mongoose placeholder models, README, .gitignore) — commit fd72a17; Authentication (signup/login/JWT, protected /me) — commit 3f440ca; Profile system (model, CRUD API, completion score, multi-step builder UI) — commit 00c29f3; Discovery + Matching (Like/Match models, discovery feed with pagination + basic filters, swipe API with mutual-match detection, matches list, discovery/match UI) — commit f86267a; Chat (Message model, GET/POST/PATCH /api/matches/:matchId/messages, Socket.IO real-time layer with JWT handshake auth reusing the shared verifyToken() helper, typing indicator, read receipts, real chat UI replacing ChatComingSoon.jsx) — commit f027d73; Notifications (Notification model, GET/PATCH/PUT /api/notifications* routes, match/like/message notification creation with per-type preference gating, live notification:new Socket.IO event via a per-user room, bell/badge + dropdown notification center, minimal Settings page for preference toggles) — see this entry's commit
Features In Progress: None currently in flight
Remaining Features: Verification, Report/Block/Safety Center, Admin panel, Subscription, AI features, Safe Date, Events, Analytics, Security hardening, Testing, Deployment. Also still deferred within already-started phases: a persisted `preferences` collection (age range/distance filters), unmatch, "who liked you" reveal screen (the notification exists but stays anonymous — see MOCK_FEATURES.md/docs/BUSINESS_PLAN.md), image/voice chat messages (V2), matches-list last-message preview/unread badge, real FCM push notification delivery (see MOCK_FEATURES.md)
Known Bugs: None recorded yet — see BUGS.md
Known Technical Debt: MongoDB connection is still not verified against a live database in this sandbox (no mongod/Docker/network access to MongoDB binaries available here — see IMPLEMENTATION_PROGRESS.md for what was and wasn't verifiable) — this now also blocks verifying the Notification indexes (in addition to the previously-noted Like/Match/Message ones) against a real database, not just pure logic; Cloudinary/Firebase/Razorpay/Claude API integrations not yet configured (no credentials yet) — Firebase specifically gates real push notification delivery, see MOCK_FEATURES.md; photo storage is a MOCK/TEMPORARY path (base64/URL stored directly on the profile doc) until Cloudinary is wired up — see MOCK_FEATURES.md; discovery has no geo/distance filtering yet (profiles.location still unpopulated) — see MOCK_FEATURES.md; chat is text-only (no attachments) — not a mock, a documented scope reduction, see MOCK_FEATURES.md; the shared frontend Socket.IO connection lifecycle changed this session (see Last Modified Files) — NotificationContext now owns connect-on-login/disconnect-on-logout instead of Chat.jsx owning connect-on-mount/disconnect-on-unmount; this is a real behavior change worth double-checking once a live DB + two real browser sessions are available, though the pure logic and a live single-socket handshake were both verified in this sandbox
Last Successful Test: Backend server boots cleanly with all six route groups (auth/profile/discovery/matches/notifications/health) and Socket.IO mounted, no syntax/import errors. Curled every new /api/notifications* route (GET /, GET /unread-count, GET/PUT /preferences, PATCH /read-all, PATCH /:id/read) with no/bad auth and confirmed 401 in all cases, plus a regression check that the pre-existing /api/matches and /api/discovery/* routes still correctly 401 with no auth (unaffected by this session's changes). A standalone Node script (38/38 checks passed, no live DB) verified: Notification schema validation (all 6 enum types accepted, missing recipient/type rejected, invalid type rejected, read/payload defaults, createdAt-only timestamps matching the Like.js convention, both declared indexes present), User.notificationPreferences defaults to {true,true,true} and validates cleanly, the pure isNotificationTypeEnabled() preference-gating helper (undefined/null/{} preferences all default-enable a type; a false field disables only its own type, not the others; verification/safety/subscription are never disable-able and have no PREFERENCE_FIELD_BY_TYPE entry to bypass), and socket.js's roomName()/userRoomName()/isUserInRoom() helpers (correct room-name formats, isUserInRoom() correctly finds/excludes users against a fake io object). A real socket.io-client was additionally used to connect a live JWT-authed socket against the running server specifically to exercise the new `socket.join(userRoomName(socket.user.id))` connect-time line for real (not just statically) — connection succeeded, no server crash, no errors in the server log. Frontend `npm run build` and `npm run lint` both pass with the new NotificationBell.jsx/NotificationContext.jsx/Settings.jsx (same one pre-existing, unrelated oxlint only-export-components warning in AuthContext.jsx carried forward, plus an identical one now in NotificationContext.jsx for the same reason — both export a hook alongside a component, a pattern already accepted in this codebase). DB-touching endpoints (actual notification persistence/pagination, actual preference-gated creation against real Like/Match/Message writes, the live notification:new broadcast reaching a real second browser session) could not be exercised end-to-end — see Known Technical Debt. TaskList tool was not available in this sandbox session to cross-check the internal task graph directly — the "Next Exact Task" below relies on the task-launch instructions' own reconciliation of the two numbering schemes instead; a future session should verify against TaskList directly once available.
Last Modified Files: backend/models/Notification.js (new), backend/constants/notificationOptions.js (new — NOTIFICATION_TYPES, PREFERENCE_FIELD_BY_TYPE, pagination defaults), backend/utils/notificationUtils.js (new — createNotification()/isNotificationTypeEnabled()/toNotificationJSON()), backend/routes/notifications.js (new — GET /, GET /unread-count, GET/PUT /preferences, PATCH /read-all, PATCH /:id/read), backend/models/User.js (added notificationPreferences sub-document), backend/socket.js (added userRoomName()/isUserInRoom(), every connected socket now auto-joins its own user:<id> room), backend/routes/discovery.js (creates match/like notifications after a swipe, isolated in try/catch), backend/routes/matches.js (creates message notifications after a send, skipped if the recipient is actively in that match's socket room, isolated in try/catch), backend/server.js (mounted /api/notifications), frontend/src/api.js (getNotifications/getUnreadNotificationCount/markNotificationRead/markAllNotificationsRead/getNotificationPreferences/updateNotificationPreferences), frontend/src/context/NotificationContext.jsx (new — unread count + notification cache, owns the shared socket connection for the authenticated session), frontend/src/components/NotificationBell.jsx (new — bell/badge + dropdown), frontend/src/pages/Settings.jsx (new — minimal preferences toggles), frontend/src/App.jsx (added /settings route), frontend/src/main.jsx (wrapped App in NotificationProvider), frontend/src/context/AuthContext.jsx (logout() now disconnects the shared socket), frontend/src/socket.js (comment update only — lifecycle now owned by NotificationContext, not Chat.jsx), frontend/src/pages/Chat.jsx (no longer disconnects the shared socket on unmount, only leaves its own match room — see the socket-lifecycle note in Known Technical Debt), frontend/src/pages/Dashboard.jsx (added NotificationBell + Settings link), frontend/src/pages/Discovery.jsx / frontend/src/pages/Matches.jsx (added NotificationBell to nav), docs/DATABASE_SCHEMA.md, docs/API_DOCUMENTATION.md, docs/ROADMAP.md, MOCK_FEATURES.md, TODO.md (see git log for the latest)
Database Status: MongoDB/Mongoose schemas implemented for User (now with notificationPreferences), Profile, Like, Match, Message, Notification (new this session, per docs/DATABASE_SCHEMA.md); no live DB connection has ever been verified in any sandbox session so far — this remains the top technical-debt item blocking real end-to-end testing
Backend Status: Express + Socket.IO server (same HTTP server) running with auth + profile + discovery + matches + notifications routes; GET/PUT /api/profile/me, GET /api/profile/:userId, POST /api/profile/me/photos, GET /api/discovery/feed, POST /api/discovery/swipe, GET /api/matches, GET/POST /api/matches/:matchId/messages, PATCH /api/matches/:matchId/messages/read, GET /api/notifications, GET /api/notifications/unread-count, PATCH /api/notifications/:id/read, PATCH /api/notifications/read-all, GET/PUT /api/notifications/preferences all implemented and auth-protected; Socket.IO events match:join/match:leave/typing:start/typing:stop (client->server) and message:new/message:read/typing/notification:new (server->client) implemented; every socket now auto-joins a personal user:<id> room in addition to whatever match:<id> rooms it explicitly joins
Frontend Status: Landing, auth pages, dashboard (profile completion + CTA + Discover/Matches nav + notification bell + Settings link), multi-step Profile Builder, Discovery (card + Like/Pass, loads more as queue runs low, "It's a Match!" modal, notification bell), Matches (list, links to the real chat screen, notification bell), Chat (message history + "load older" cursor pagination, send, live delivery via Socket.IO, typing indicator, read-receipt checkmarks), Settings (minimal — notification preference toggles) are all live; design-system color tokens (light + dark, from docs/DESIGN_SYSTEM.md) wired into Tailwind v4 via frontend/src/index.css; Button/TextField/Avatar/MatchModal/ChatBubble/NotificationBell reusable components under frontend/src/components/
Authentication Status: Implemented (signup/login/JWT/me endpoint) — unchanged in this session
AI Status: Not started (planned: Claude API, backend-only)
Payment Status: Not started (planned: Razorpay, configurable plans)
Admin Status: Not started
Deployment Status: Not started (planned: Render/Railway + MongoDB Atlas + Vercel/Netlify + Cloudinary)
Next Exact Task: Task #9 — Verification (mobile + photo), per the task-launch instructions' own reconciliation of the two independent numbering schemes in play (an internal TaskList tool with IDs #1-#12, where #8 was this Notifications task and #9 is Verification; and docs/ROADMAP.md's own Phase 0-16 numbering, where Notifications = Phase 6 and Verification = Phase 7). This sandbox session did not have the TaskList tool available to double-check the internal graph directly, so this line is written in words rather than a bare number, per the same instructions — whoever picks this up next should still confirm against TaskList if it's available to them, in case another agent changed the graph in the meantime. Scope per docs/API_DOCUMENTATION.md's `[PLANNED]` Verification section and docs/DATABASE_SCHEMA.md's `verifications` collection: mobile OTP verification (shared with signup, POST /api/verification/otp/request + /verify, marks users.isPhoneVerified), a selfie/photo verification flow (POST /api/verification/selfie, creates a PENDING verifications record — no real human-review queue needed yet, that's Task #9 (Admin, internal numbering) / Phase 9 (Admin, docs/ROADMAP.md numbering)), GET /api/verification/status, and a verification badge surfaced on profiles (profiles.isPrimaryPhotoVerified per docs/DATABASE_SCHEMA.md's `profiles` section, which already notes this field is deferred until Verification lands). OTP delivery (SMS) itself will very likely need to be MOCK/TEMPORARY (no SMS provider credentials configured, same pattern as Cloudinary/FCM/Razorpay) — mark that clearly in MOCK_FEATURES.md if so, but the OTP generation/storage/verification logic and the selfie-review record-keeping should be real.
Next Recommended Action: Start Task #9 (Verification). It can reuse this session's Notification infrastructure directly — a 'verification' notification type already exists in the schema/enum (backend/constants/notificationOptions.js) with no code path creating it yet, specifically reserved for this: notify the user when their selfie verification is approved/rejected. Note that 'verification' is deliberately NOT in PREFERENCE_FIELD_BY_TYPE (not user-disable-able), which is correct behavior to preserve — don't add a preference toggle for it without reconsidering that choice first. Before or alongside it, get a real MongoDB connection verified in whatever environment will run this next (this sandbox still cannot reach any MongoDB — no local mongod, no Docker daemon, and mongodb-memory-server's binary download was blocked by the outbound proxy) so Notifications' actual DB-backed behavior (preference-gated creation against real writes, pagination against real data, the live notification:new broadcast reaching a real second browser session, the new socket connection lifecycle change working correctly across a real login->navigate->logout cycle) can finally be exercised end-to-end instead of only via server-boot + pure-logic + live-single-socket-handshake checks.
